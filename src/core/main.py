def say_hello(name:str = "world"):
    print(f'hello {name}')

"""Core entry points and utilities."""

def add(a: float, b: float) -> float:
    """Return the sum of ``a`` and ``b``."""
    return a + b
